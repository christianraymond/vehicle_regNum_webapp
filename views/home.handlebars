<!DOCTYPE html>
<html lang="en">

<head>
  <title></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="../css/registration_number.css">

  <body>
    <h2><kbd>CAR REGISTRATION NUMBER WEB APPLICATION UP AND RUNNING</kbd></h2>
    <div class="main-page">
    </div>
    <h6>ADD HERE</h6>
    <form class="" action="/add" method="post">
      <input type="text" name="plate" placeholder="Search plate number here" required>
      <input type="submit" name="plateNumber" value="Add" required><br></br>
    </form>

    <form class="" action="/doFilter" method="post">
      <input type="radio" name="loc" value="CA" required>CapeTown
      <input type="radio" name="loc" value="CY" required>Bellevile
      <input type="radio" name="loc" value="CP" required>Paarl <br></br>
      <input type="submit" name="filterReg" value="Filter" required>
    </form>
    </div><br></br>
  </body>
</head>

{{#if messages.error}}
  <div class="error">
    {{messages.error}}
    <br>
    <br>
  </div>
{{/if}}
{{#if messages.success}}
  <div class="success">
    {{messages.success}}
    <br>
    <br>
  </div>
{{/if}}
<b>
{{status}}
       </b>
<ul>
  {{#each plates}}
    <li>{{this.plateNumber}}</li>
  {{/each}}

  {{#each filterReg}}
    <li>{{this.filterReg}}</li>
  {{/each}}
</ul>
</div>
